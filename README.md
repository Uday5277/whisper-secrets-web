# 🤫 Whisper Secrets

A simple, stylish web application built during my web development journey. It connects to the App Brewery Secrets API to fetch and display random anonymous secrets.

## ✨ Features
* **Real-time API Integration:** Fetches data from an external REST API using Axios.
* **Dynamic Templating:** Uses EJS to render secrets and usernames dynamically.
* **Custom UI:** Features a "glowing" CSS card effect with custom `@property` rotations and Google Fonts.
* **Responsive Backend:** Built with Express.js for a lightweight and fast server-side experience.

## 🚀 Tech Stack
* **Frontend:** HTML5, CSS3, EJS
* **Backend:** Node.js, Express.js
* **HTTP Client:** Axios

## 🛠️ Installation & Setup
1. **Clone the repository:**
   ```bash
   git clone https://github.com/Uday5277/whisper-secrets-web.git
   ```

2.**Install dependencies:**
   ```bash
   npm install
  ```

3.**Run the server:**
  ```bash
  node index.js
  ```

4.**View the app:**
Open your browser and navigate to http://localhost:3000
